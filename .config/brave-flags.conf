# Brave is Chromium (not Electron) — ELECTRON_OZONE_PLATFORM_HINT doesn't
# apply, so without this it runs XWayland and renders 1.5x small under
# xwayland:force_zero_scaling.
--ozone-platform-hint=auto
